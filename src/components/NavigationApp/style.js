import styled from "styled-components";
import { motion } from "framer-motion";

export const ListNavigation = styled(motion.ul)`
  margin: 0;
  padding: 0;
  padding: 25px;
  position: absolute;
  top: 100px;
  width: 230px;
`;
